{% extends 'main.twig' %}

{% block content %}
    <div class="container-fluid my-5">
        <div class="row">
            <div class="col text-center align-middle h-100 justify-content-center align-items-center">
                <h1>{{ title }}</h1>
                <h5>{{ description }}</h5>
                <button class="btn btn-outline-info mt-3" onclick="history.back()">Gå tilbake</button>
            </div>
        </div>
    </div>
{% endblock %}